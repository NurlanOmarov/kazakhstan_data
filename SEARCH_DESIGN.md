# Параметры поиска и работа с качеством данных

## По каким параметрам ищет пользователь

| Параметр | Как ищем | Почему так |
|---|---|---|
| **ИИН** | Точное совпадение по индексу `idx_inn` | 12 цифр, уникален, заполнен почти везде — самый точный ключ |
| **ФИО** (Фамилия/Имя/Отчество) | FTS5 префикс + нечёткий поиск (Левенштейн) | Можно вводить частично, с опечатками; ~23% записей без отчества |
| **ФИО одной строкой** | Каждый токен ищется как префикс по любому из полей ФИО | Удобно: пользователь вводит «Сыздыкова Багыжан» в одно поле |
| **Телефон** | Нормализация в цифры (8→7), префиксный поиск по `*_normalized` | В базе формат `+7(XXX)...`, несколько номеров через запятую |
| **Дата рождения** | Префиксный LIKE (`ГГГГ`, `ГГГГ-ММ`, полная дата) | Единый ISO-формат; удобно сузить выдачу по году |
| **Адрес** | FTS5 префикс по токенам | Адрес — структурированный текст (ГОРОД/УЛИЦА/ДОМ) |

Несколько полей объединяются по **И** (AND) — это главный способ снять неоднозначность
(например, однофамильцев), особенно когда отчество отсутствует.

## Учёт ошибок В САМОЙ базе (качество данных)

Данные собраны из разных источников, поэтому поиск устойчив к их дефектам:

1. **Регистр и варианты букв.** ФИО в верхнем регистре, есть казахские буквы.
   Нормализация приводит обе стороны к единому виду: верхний регистр,
   `Ә→А, Ғ→Г, Қ→К, Ң→Н, Ө→О, Ұ/Ү→У, Һ→Х, І→И, Ё→Е, Й→И`.
   → «Өмір», «Омир», «омир» находят одно и то же.

2. **Опечатки в базе** (напр. «РЫСБАЕВНА» вместо «РЫСПАЕВНА»).
   Если точный поиск ничего не дал — включается нечёткий: берём надёжный
   префикс одного поля, затем фильтруем кандидатов по расстоянию Левенштейна
   (допуск растёт с длиной слова). → опечатка в 1–2 буквы не мешает найти.

3. **Разный формат телефонов** (`+7(...)`, `8...`, дубли через запятую).
   При импорте они уже приведены к строке цифр; ввод пользователя
   нормализуется так же (8→7) → формат не важен.

4. **Пропуски** (≈23% без отчества, часть без телефона/адреса).
   Поиск не требует заполнения всех полей; пустые поля игнорируются,
   отсутствие отчества не отсекает запись.

5. **Мусорные значения** (`.`, 2-буквенные имена, ИИН неверной длины).
   Не ломают поиск — просто не совпадают; точный ИИН-поиск работает по факту.

6. **Неоднозначность.** В ответе возвращается `mode`:
   `exact` — точное совпадение, `fuzzy` — найдено с поправкой на опечатку
   (UI это подсвечивает, чтобы пользователь понимал, что сработал нечёткий поиск).

## Идеи на будущее
- Транслитерация лат↔кир (если встречаются записи латиницей).
- Фонетический поиск (Soundex/Metaphone, адаптированный под казахский).
- Объединение дублей-«почти-одинаковых» записей одного человека.
